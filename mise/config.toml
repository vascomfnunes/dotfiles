[settings]

[settings.ruby]
# Prefer prebuilt Ruby installs where available.
compile = false

[tools]
# Core runtimes available in every shell through `mise activate`.
ruby = "3.3.9"
node = "22.22.3"
# Pinned to a minor version so fresh installs are reproducible.
python = "3.13"
